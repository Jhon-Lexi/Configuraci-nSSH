ssh-keygen -t ed25519 -c "correo"
eval "$(ssh-agent -s)"
ssh-add ~/ .ssh/id_ed25519
ssh-add -l -E sha256
clip < ~/.ssh/id_25519

git remote -v
git remote rm origin

git status "ver estado del repositorio"
git commit -m "para comitear el repositorio"